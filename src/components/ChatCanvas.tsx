// src/components/ChatCanvas.tsx
import React from 'react';
import styles from './ChatCanvas.module.css'; // Import the CSS module

// Placeholder component for the canvas area
export const ChatCanvas = () => {
  return (
    // Apply container style from the module
    <div className={styles.canvasContainer}>
      {/* Placeholder Canvas Area */}
      {/* Apply placeholder style from the module */}
      <div className={styles.canvasPlaceholder}>
        Canvas Area
      </div>
    </div>
  );
};